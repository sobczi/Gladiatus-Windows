if (!first && quality[i] == first_quality) { continue; }
                else if (!first && quality[i] != first_quality) { another_quality = true; break; }

                if (first && quality[i] != "*")
                { first_quality = quality[i]; first = false; }